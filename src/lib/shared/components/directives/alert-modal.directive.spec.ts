import { AlertModalDirective } from './alert-modal.directive';

describe('AlertModalDirective', () => {
  it('should create an instance', () => {
    const directive = new AlertModalDirective();
    expect(directive).toBeTruthy();
  });
});
